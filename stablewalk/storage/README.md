# Session storage (Save Session feature)

Persistent SQLite storage for kinematic and biomechanical data collected during
video playback. Uses the Python standard library only (`sqlite3`).

See `docs/SESSION_STORAGE.md` for architecture and usage.
